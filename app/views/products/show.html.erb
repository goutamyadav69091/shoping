<h1><%= @product.name %></h1>

<span>Price => </span>  <span><%= @product.price %></span><br>
<span>type => </span>  <span><%= @product.product_type %></span><br>
<span>Creater => </span>  <span><%= @product.user.name %></span><br>

<ul>
  <li><%= link_to "Edit", edit_product_path(@product) %></li>
  <li><%= link_to "Destroy", product_path(@product), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>
<span><%= alert %></span>